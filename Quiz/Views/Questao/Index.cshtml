@model IEnumerable<Quiz.Models.Questao>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Pergunta)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Opcao_a)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Opcao_b)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Opcao_c)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Opcao_d)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Resposta_certa)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Pergunta)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Opcao_a)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Opcao_b)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Opcao_c)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Opcao_d)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Resposta_certa)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
